\documentclass[12pt,a4paper]{article}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage{geometry}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{booktabs}
\usepackage{array}
\usepackage{graphicx}
\usepackage{float}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{enumitem}
\usepackage{xcolor}
\usepackage{hyperref}
\usepackage[numbers,sort&compress]{natbib}

\geometry{margin=2.45cm}
\hypersetup{
  colorlinks=true,
  linkcolor=blue!55!black,
  citecolor=blue!55!black,
  urlcolor=blue!55!black
}
\graphicspath{{figures/}}
\setlist[itemize]{topsep=3pt,itemsep=2pt,parsep=0pt}
\setlist[enumerate]{topsep=3pt,itemsep=2pt,parsep=0pt}
\emergencystretch=2em

\newcommand{\LCDM}{\ensuremath{\Lambda{\rm CDM}}}
\newcommand{\Hz}{\ensuremath{H_0}}
\newcommand{\Om}{\ensuremath{\Omega_{\rm m0}}}
\newcommand{\chisq}{\ensuremath{\chi^2}}
\newcommand{\dd}{\mathrm{d}}
\newcommand{\repo}{\url{https://github.com/MartinPetrasek123/MartinPetrasek123/tree/main/r-universe-complete-chain}}
\newcommand{\release}{\texttt{v1.1.0}}

\title{\textbf{R-Universe as a Testable Completion of Flat \texorpdfstring{\LCDM}{LCDM}:\\
Physical Principle, Background Equations, Perturbative Closure, Public Data Fits, and Falsifiable Predictions}}

\author{
Martin Petrášek\\
\small Institute for Resonant Synthesis and Field Physics (IRSFP), Prague, Czech Republic\\
\small ORCID: \href{https://orcid.org/0009-0007-4512-9528}{0009-0007-4512-9528}
}

\date{Draft version: 19 July 2026}

\begin{document}

\maketitle

\begin{abstract}
Flat \LCDM is an exceptionally successful empirical model, but it leaves the observed vacuum component as a rigid parameter rather than an explained infrared quantity. This paper presents a complete reproducible late-time calculation for a minimal R-Universe extension. The physical premise is that the locally gravitating late-time vacuum sector is an infrared effective component constrained by global or renormalization-group structure, rather than the raw additive zero-point energy. The minimal branch R1 is defined by
\[
E^2(z)-(1-\Omega_{\rm m0})E^{2-\theta}(z)-\Omega_{\rm m0}(1+z)^3=0,
\]
and contains flat \LCDM exactly and regularly at $\theta=2$. A second stress branch R2,
\[
E^2(z)-(1-\Omega_{\rm m0})(1+z)^\nu E^{2-\theta}(z)-\Omega_{\rm m0}(1+z)^3=0,
\]
tests whether additional shape freedom is statistically justified. The full chain is calculated explicitly: physical principle, background dynamics, supernova and BAO distances, analytic supernova-offset marginalization, full-covariance DESI BAO likelihood, cosmic chronometer likelihood, DES-Dovekie cross-check, minimal-GR growth closure, cosmographic diagnostics, Sandage--Loeb drift and an ISW-source proxy. For Pantheon+ full covariance + DESI DR2 BAO + cosmic chronometers, R1 gives $\Delta\chi^2=-3.7117$, $\Delta{\rm AIC}=-1.7117$ and $\Delta{\rm BIC}=+3.6809$ relative to \LCDM. For DES-Dovekie STAT+SYS + DESI DR2 BAO + chronometers, R1 gives $\Delta\chi^2=-4.2133$, $\Delta{\rm AIC}=-2.2133$ and $\Delta{\rm BIC}=+3.3172$. R1 is therefore repeatedly AIC-favored in public late-time data, but not yet BIC-favored and not yet a replacement for a full Boltzmann/CMB implementation. All results are obtained within a public, versioned and fully reproducible analysis framework.
\end{abstract}

\noindent\textbf{Keywords:}
R-Universe; \LCDM; dark energy; renormalization group; vacuum energy; Pantheon+; DESI DR2; DES-Dovekie; baryon acoustic oscillations; cosmic chronometers; growth of structure; AIC; BIC.

\section{Introduction}

The \LCDM model accurately describes the cosmic microwave background, baryon acoustic oscillations, Type Ia supernovae and large-scale structure \citep{Planck2018VI,DESI2024VI,Brout2022PantheonPlus}. Its success, however, does not explain why the vacuum sector should be a perfectly rigid cosmological constant. The cosmological constant problem remains a structural problem: a constant shift of the matter Lagrangian changes the stress tensor by a metric-proportional vacuum term and should gravitate in ordinary local Einstein equations \citep{Weinberg1989}.

R-Universe is tested here as a conservative but falsifiable late-time completion. It does not assume that local general relativity fails in tested regimes. Instead, it asks whether the cosmological vacuum term entering the homogeneous infrared background can be an effective component generated by a global constraint or renormalization-group flow. The model is deliberately constructed so that \LCDM is recovered exactly. This makes the statistical question sharp: does the one-parameter deformation improve public data enough to pay for its extra parameter?

The paper is organized as a complete calculation rather than a qualitative proposal. The chain is:
\[
\hbox{principle}\rightarrow
\hbox{background equation}\rightarrow
\hbox{observables}\rightarrow
\hbox{likelihoods}\rightarrow
\hbox{growth closure}\rightarrow
\hbox{statistics}\rightarrow
\hbox{predictions}.
\]
No fit number in this manuscript is quoted without a code path. The local and GitHub reproducibility package contains:
\begin{itemize}
\item \texttt{code/extended\_fit.py}: Pantheon+ full-covariance + DESI DR1/DR2 + chronometer fits;
\item \texttt{code/des\_dovekie\_fit.py}: DES-Dovekie STAT+SYS + DESI DR2 + chronometer cross-check;
\item \texttt{code/derived\_predictions.py}: cosmographic, growth and null-test predictions;
\item \texttt{code/make\_figures\_and\_tables.py}: figure and table generation;
\item \texttt{tables/all\_model\_fits.csv}: all fitted model summaries;
\item \texttt{tables/desi\_dr2\_bao\_predictions.csv}: BAO predictions and diagnostic pulls;
\item \texttt{data\_manifest/data\_sources.md}: public source URLs and local file mapping.
\end{itemize}

\section{Physical principle}

\subsection{Vacuum energy as an infrared observable}

The physical premise is not that arbitrary model flexibility should be added to \LCDM. It is that the observed vacuum component may be an infrared observable rather than the raw microscopic vacuum energy. Vacuum-energy sequestering frameworks show how global variables and constraints can remove sensitivity of local curvature to additive vacuum-energy shifts \citep{KaloperPadilla2014,KaloperPadilla2015}. R-Universe uses this as motivation, not as a completed derivation: the empirical model below is self-contained and can be falsified without assuming a unique microscopic sequestering implementation.

\subsection{Renormalization-group scaling}

In asymptotic-safety and functional-renormalization approaches, the effective gravitational action depends on a coarse-graining scale $k$ \citep{Reuter1998,ReuterSaueressig2019,BonannoSaueressig2017,Platania2020}. A dimensionless cosmological coupling $\lambda(k)=\Lambda(k)/k^2$ linearized around a fixed point satisfies schematically
\begin{equation}
k\frac{\dd}{\dd k}(\lambda-\lambda_*)=-\theta_{\rm RG}(\lambda-\lambda_*),
\end{equation}
so that
\begin{equation}
\Lambda(k)=\lambda_*k^2+Ck^{2-\theta_{\rm RG}}.
\end{equation}
Identifying the late-time infrared scale with $k\propto H$ motivates an effective component
\begin{equation}
\rho_R(H)=\rho_{R0}E^{2-\theta}.
\label{eq:rhoR}
\end{equation}
The observational exponent $\theta$ is treated as an effective exponent. A future fundamental derivation must compute it from a specified truncation, regulator, gauge choice and stability matrix. This paper does not hide that missing step; it separates the empirical R1 calculation from the ultraviolet derivation.

\section{Background equations}

Assume a spatially flat late-time background and conserved pressureless matter,
\begin{equation}
\rho_m(z)=\rho_{m0}(1+z)^3.
\end{equation}
Using Eq.~\eqref{eq:rhoR}, the normalized Friedmann equation becomes
\begin{equation}
E^2(z)=\Omega_{\rm m0}(1+z)^3+(1-\Omega_{\rm m0})E^{2-\theta}(z).
\label{eq:r1}
\end{equation}
Equivalently,
\begin{equation}
F(E,z;\Omega_{\rm m0},\theta)=
E^2-(1-\Omega_{\rm m0})E^{2-\theta}
-\Omega_{\rm m0}(1+z)^3=0.
\end{equation}

\subsection{Exact embedding of \LCDM}

For $\theta=2$,
\begin{equation}
E^{2-\theta}=1,
\end{equation}
and Eq.~\eqref{eq:r1} becomes
\begin{equation}
E^2(z)=\Omega_{\rm m0}(1+z)^3+1-\Omega_{\rm m0},
\end{equation}
which is exactly flat \LCDM. Thus
\[
{\cal M}_{\Lambda{\rm CDM}}\subset{\cal M}_{\rm R1}
\]
at the background level. The limit is regular on the physical branch because
\begin{equation}
\frac{\partial F}{\partial E}
=2E-(1-\Omega_{\rm m0})(2-\theta)E^{1-\theta}
\rightarrow 2E>0
\end{equation}
at $\theta=2$ for $E>0$. The numerical solver therefore follows the positive branch connected to \LCDM. This is implemented in \texttt{code/extended\_fit.py}.

\subsection{R2 stress branch}

The R2 stress branch is
\begin{equation}
E^2-(1-\Omega_{\rm m0})(1+z)^\nu E^{2-\theta}
-\Omega_{\rm m0}(1+z)^3=0.
\label{eq:r2}
\end{equation}
It has one more parameter than R1 and two more than \LCDM. It is not treated as the preferred theory. It is a control experiment: if lower $\chi^2$ is bought only by extra flexibility, BIC should penalize it.

\section{Effective conservation and derived background quantities}

The effective R-sector density is
\begin{equation}
\rho_R(z)=\rho_{R0}E^{2-\theta}(z).
\end{equation}
If reconstructed as an effective fluid, its background equation of state is
\begin{equation}
w_R(z)=-1+\frac{2-\theta}{3}(1+z)\frac{E'(z)}{E(z)}.
\label{eq:wR}
\end{equation}
Implicit differentiation of R1 gives
\begin{equation}
E'(z)=
\frac{3\Omega_{\rm m0}(1+z)^2}
{2E-(1-\Omega_{\rm m0})(2-\theta)E^{1-\theta}}.
\label{eq:eprime}
\end{equation}
The main R1 fit gives $w_R(0)=-0.9347$ and $w_R(1)=-0.8525$, computed by \texttt{code/derived\_predictions.py}.

The deceleration and jerk are
\begin{align}
q(z)&=-1+(1+z)\frac{E'(z)}{E(z)},\\
j(z)&=q+2q^2+(1+z)\frac{\dd q}{\dd z}.
\end{align}
The code returns $j_0=0.999998$ for \LCDM, validating the numerical derivative, and $j_0=0.93419$ for the main R1 branch.

The future de Sitter limit for R1 is obtained by taking $z\rightarrow-1$:
\begin{equation}
\frac{H_\infty}{H_0}=(1-\Omega_{\rm m0})^{1/\theta}.
\end{equation}
For the main R1 fit, $H_\infty/H_0=0.80695$.

\section{Distances and likelihood observables}

The comoving distance is
\begin{equation}
D_C(z)=\frac{c}{H_0}\int_0^z\frac{\dd z'}{E(z')}.
\end{equation}
For a flat background,
\begin{align}
D_M(z)&=D_C(z),\\
D_H(z)&=\frac{c}{H_0E(z)},\\
D_V(z)&=\left[zD_M^2(z)D_H(z)\right]^{1/3}.
\end{align}
All BAO fits fix
\begin{equation}
r_d=147.09\ {\rm Mpc}.
\end{equation}
This is a late-time test. A future complete early-universe model must compute $r_d$.

For supernovae,
\begin{equation}
\mu_{\rm th}=5\log_{10}(D_L/{\rm Mpc})+25,
\end{equation}
with
\begin{equation}
D_L=(1+z_{\rm hel})D_C(z_{\rm HD})
\end{equation}
for Pantheon+. The absolute magnitude/offset is analytically marginalized:
\begin{equation}
\chi^2_{\rm SN}=r^TC^{-1}r-
\frac{(1^TC^{-1}r)^2}{1^TC^{-1}1}.
\label{eq:snchi}
\end{equation}

BAO and chronometer likelihoods are
\begin{align}
\chi^2_{\rm BAO}&=\Delta_{\rm BAO}^TC_{\rm BAO}^{-1}\Delta_{\rm BAO},\\
\chi^2_{\rm CC}&=\sum_i\left[\frac{H_i-H_0E(z_i)}{\sigma_{H,i}}\right]^2.
\label{eq:baocc}
\end{align}
The total statistic is
\begin{equation}
\chi^2_{\rm tot}=\chi^2_{\rm SN}+\chi^2_{\rm BAO}+\chi^2_{\rm CC}.
\end{equation}

\section{Perturbative and growth closure}

A full perturbation theory requires an action or complete field equations. The present paper therefore states the minimal closure used for a complete late-time consistency calculation:
\begin{enumerate}
\item pressureless matter is conserved and minimally coupled;
\item the R-sector is smooth on the subhorizon scales used in the compressed growth check;
\item $G_{\rm eff}=G$ in the Poisson equation;
\item the background expansion is given by R1 or R2.
\end{enumerate}
Under this closure, matter growth satisfies
\begin{equation}
\frac{\dd^2D}{\dd\ln a^2}
\,+\,
\left[2+\frac{\dd\ln H}{\dd\ln a}\right]\frac{\dd D}{\dd\ln a}
-\frac{3}{2}\Omega_m(a)D=0,
\label{eq:growth}
\end{equation}
where
\begin{equation}
\Omega_m(a)=\frac{\Omega_{\rm m0}a^{-3}}{E^2(a)}.
\end{equation}
The solver starts at $a=10^{-3}$ with $D\propto a$ and normalizes to $D(0)=1$. The main R1 branch gives a maximum suppression of the normalized $fD$ proxy relative to \LCDM of $3.42\%$ at $z\simeq0.33$.

For a CMB-relevant proxy, the minimal GR Weyl-potential scaling is
\begin{equation}
\Phi(z)\propto D(z)(1+z).
\end{equation}
The late ISW source is proportional to its time derivative. The code computes a redshift-derivative proxy and obtains, at $z=1$,
\[
{\cal I}_{\rm R1}=0.1011,\qquad
{\cal I}_{\Lambda{\rm CDM}}=0.0779.
\]
This is a falsifiable proxy, not a full CMB likelihood.

\section{Public data sources and exact data provenance}
\label{sec:data-provenance}

This section is written as an audit trail. Each likelihood block identifies the public source, exact local input files, covariance treatment, selection mask, data-vector size, transformation to theory space and the script that performs the calculation. The same information is mirrored in \texttt{data\_manifest/data\_sources.md}. No numerical result in the paper is produced by a hidden notebook.

\subsection{Pantheon+ supernova sample}

Pantheon+ is taken from the public PantheonPlusSH0ES release \citep{PantheonPlusDataRelease,Brout2022PantheonPlus}. The fit uses the public distance-modulus table and the full statistical-plus-systematic covariance:
\begin{itemize}
\item input table: \texttt{pantheon\_plus.dat};
\item covariance: \texttt{pantheon\_plus\_stat\_sys.cov};
\item SHA256(input table): \texttt{1cb0fc379ef066afdc2ffd1857681cc478024570d8a3eba284fb645775198cf8};
\item SHA256(covariance): \texttt{abf806d966485e64afdb359c87bffc0ecc00d05eff0a31ced66f247385df0fdc};
\item script: \texttt{code/extended\_fit.py}.
\end{itemize}
The code removes Cepheid calibrators and applies $z_{\rm HD}>0.01$. The retained sample contains $N_{\rm SN}=1580$ supernovae. The data vector is the observed distance-modulus residual after analytic marginalization over an additive magnitude/Hubble intercept. The covariance is restricted to the same mask before inversion. The supernova contribution is Eq.~\eqref{eq:snchi}, so the nuisance offset never appears as a fitted parameter in the AIC/BIC count.

The explicit computational path is
\[
\hbox{Pantheon+ table}
\rightarrow
\hbox{Cepheid and }z_{\rm HD}\hbox{ mask}
\rightarrow
\{\mu_{\rm obs},z_{\rm HD},z_{\rm hel}\}
\rightarrow
C_{\rm SN}^{-1}
\rightarrow
\mu_{\rm th}(H_0,\Omega_{\rm m0},\theta,\nu)
\rightarrow
\chi^2_{\rm SN}.
\]

\subsection{DES-Dovekie / DES-SN5YR cross-check}

DES-Dovekie is taken from the public DES-SN5YR release \citep{DES2024SN5YR,Sanchez2024DESSN,DESSN5YRRepo}. It is used as an independent supernova cross-check, not combined with Pantheon+, because the low-redshift anchor and calibration systematics may overlap. The fit uses:
\begin{itemize}
\item input table: \texttt{DES-Dovekie\_HD.csv};
\item covariance: \texttt{STAT+SYS.npz};
\item metadata file used for audit: \texttt{DES-Dovekie\_Metadata.csv};
\item SHA256(input table): \texttt{2f57019d783eaa976df80a41b0054171a2d994ee9808d715ce850c2df5720aaf};
\item SHA256(covariance): \texttt{ffd3124b32148b1372bd95fda9299269f0352a9f8eee02d416c610e38495463b};
\item script: \texttt{code/des\_dovekie\_fit.py}.
\end{itemize}
The DES likelihood uses the published binned Hubble diagram and the supplied STAT+SYS covariance. The same additive supernova offset is analytically marginalized. The final combined vector has $N=1864$ entries after adding DESI DR2 BAO and chronometers; the supernova block contributes $N_{\rm DES}=1820$ distance points in the implemented likelihood.

\subsection{DESI DR1 and DR2 BAO}

DESI BAO data are taken from public DESI DR1/DR2 measurements \citep{DESI2024VI,DESIDR2BAO} through \texttt{CobayaSampler/bao\_data} \citep{CobayaBAOData}. The main result uses the DESI DR2 all-tracer Gaussian BAO vector:
\begin{itemize}
\item DR2 mean vector: \texttt{desi\_bao\_dr2/desi\_gaussian\_bao\_ALL\_GCcomb\_mean.txt};
\item DR2 covariance: \texttt{desi\_bao\_dr2/desi\_gaussian\_bao\_ALL\_GCcomb\_cov.txt};
\item SHA256(DR2 mean): \texttt{9ac154ab583ce759c0f7eef3c978c7c70a6ead2d18774caceadf1a350a640585};
\item SHA256(DR2 covariance): \texttt{252a143274c8a07c78694c119617d36594f6d7965d00319ca611c6ffb886e509};
\item DR1 mean vector: \texttt{desi\_2024\_bao\_mean.txt};
\item DR1 covariance: \texttt{desi\_2024\_bao\_cov.txt};
\item SHA256(DR1 mean): \texttt{dd2873a0b88459a491af3c0c0307ba059f62df9211d5b976760f310565a1be68};
\item SHA256(DR1 covariance): \texttt{bbafa9074b51cf1a45e0d10e4f37db8c0e80a5d1d1788857abb7fc49fb21abcc};
\item script: \texttt{code/extended\_fit.py}.
\end{itemize}
The DR2 vector order is explicitly reproduced in \texttt{tables/desi\_dr2\_bao\_predictions.csv}: $D_V/r_d$ at $z=0.295$, $D_M/r_d$ and $D_H/r_d$ at $z=0.510,0.706,0.934,1.321,1.484$, and $D_H/r_d,D_M/r_d$ at $z=2.330$. The covariance is used as a full matrix in
\[
\Delta_{\rm BAO}^TC_{\rm BAO}^{-1}\Delta_{\rm BAO}.
\]
The sound horizon is fixed to $r_d=147.09\,{\rm Mpc}$ in all late-time comparisons. This is deliberately counted as a limitation: a complete early-universe R-sector calculation must predict $r_d$ rather than import it.

\subsection{Cosmic chronometers}

The chronometer block contains 31 published $H(z)$ points represented explicitly inside \texttt{code/extended\_fit.py} and \texttt{code/des\_dovekie\_fit.py}. The compilation includes measurements from Simon, Stern, Moresco, Zhang, Ratsimbazafy and later literature, with the numerical triplets $(z,H,\sigma_H)$ visible in the code. No covariance between chronometer points is assumed, so the implemented likelihood is the diagonal expression in Eq.~\eqref{eq:baocc}. This choice is conservative for transparency and must be upgraded if a published correlated chronometer covariance is adopted.

The computational path is
\[
\{z_i,H_i,\sigma_{H,i}\}
\rightarrow
H_{\rm th}(z_i)=H_0E(z_i)
\rightarrow
\chi^2_{\rm CC}
\rightarrow
\chi^2_{\rm tot}.
\]

\subsection{Growth and lensing compressed checks}

Growth checks use the Sagredo--Nesseris--Sapone $f\sigma_8$ compilation and compressed weak-lensing values from DES Y3 and KiDS-1000 \citep{Sagredo2018RSD,Amon2022DESY3,Asgari2021KiDS1000}. The hashes of the local audit files are:
\begin{itemize}
\item \texttt{rsd\_fsigma8\_sagredo2018\_plus\_boss2024.csv}: \texttt{fe6b4e2e488991751e24d21d3c0a9065a5b1767b1d56cf3fae01c157f95f6e4d};
\item \texttt{lensing\_s8\_compressed.csv}: \texttt{f2258e1c7b87241190bbb5bb32d1ea01c4ed992d30374820357a541df33e14f2}.
\end{itemize}
The present manuscript does not claim a full RSD or weak-lensing likelihood. It computes the minimal-GR growth closure and reports falsifiable changes in $D(z)$, $f(z)$, $fD$ and the ISW-source proxy from \texttt{code/derived\_predictions.py}. This prevents the text from overstating what has been calculated.

\section{Computational reproducibility protocol}

\subsection{Software environment}

The calculations were rerun in the Codex bundled scientific Python environment on macOS 26.5.2 arm64, using Python 3.12.13, \texttt{numpy} 2.3.5, \texttt{pandas} 2.2.3, \texttt{matplotlib} 3.11.0, \texttt{scipy} 1.18.0 and \texttt{camb} 2.0.0. The environment is recorded in \texttt{requirements.txt} and \texttt{environment.yml}. No private database, GUI operation or manual spreadsheet manipulation is required.

\subsection{Repository layout}

The public package is organized as:
\begin{verbatim}
r-universe-complete-chain/
  main.tex
  references.bib
  graf.tex
  code/
  data/raw/
  data_manifest/
  figures/
  tables/
\end{verbatim}
The released package includes the exact small public data products used by the scripts under \texttt{data/raw/}. The manifest records their public source URLs, exact filenames, version/access information, license status and SHA256 checksums.

\subsection{Execution order}

The full late-time analysis is reproduced by:
\begin{verbatim}
python3 code/extended_fit.py
python3 code/des_dovekie_fit.py
python3 code/derived_predictions.py
python3 code/numerical_validation.py
python3 code/boltzmann_camb.py
python3 code/make_figures_and_tables.py
\end{verbatim}
The first script generates the Pantheon+ full-covariance + DESI DR1/DR2 + chronometer fits. The second generates the DES-Dovekie cross-check. The third computes cosmographic, growth and null-test predictions. The fourth validates the numerical solver independently. The fifth computes the CAMB reference early-universe sector and writes CMB and matter-power outputs. The final script regenerates all figures and CSV tables. The output JSON files are not independent evidence; they are machine-readable records of the preceding solver results.

\subsection{Global calculation flow}

The complete numerical flow is
\[
\hbox{public data files}
\rightarrow
\hbox{dataset-specific masks}
\rightarrow
\hbox{theory observables}
\rightarrow
\hbox{block likelihoods}
\rightarrow
\chi^2_{\rm tot}
\rightarrow
\hbox{grid/refined minimization}
\rightarrow
\hbox{AIC/BIC}
\rightarrow
\hbox{CSV/JSON}
\rightarrow
\hbox{figures and tables}.
\]
For a proposed replacement of \LCDM, this is still not the end of the chain: the required next step is a full Boltzmann implementation with CMB likelihoods and global sampling. The present work closes the late-time public-data calculation; it does not pretend that the early-universe calculation has already been solved.

\subsection{Figure and table provenance}

\begin{table}[H]
\centering
\caption{Audit trail from output objects to scripts and numerical sources.}
\begin{tabular}{p{0.18\linewidth}p{0.25\linewidth}p{0.28\linewidth}p{0.19\linewidth}}
\toprule
Object & Script & Numerical source & Output file\\
\midrule
Table 1--3 & \texttt{extended\_fit.py}, \texttt{des\_dovekie\_fit.py} & Pantheon+, DES-Dovekie, DESI DR1/DR2, chronometers & \texttt{tables/all\_model\_fits.csv}\\
Fig. 1 & \texttt{make\_figures\_and\_tables.py} & \texttt{code/extended\_results.json} & \texttt{fig01\_expansion\_Ez.pdf}\\
Fig. 2 & \texttt{make\_figures\_and\_tables.py} & best-fit $E(z)$ functions & \texttt{fig02\_om\_diagnostic.pdf}\\
Fig. 3 & \texttt{make\_figures\_and\_tables.py} & DESI DR2 vector/covariance and best fits & \texttt{desi\_dr2\_bao\_predictions.csv}\\
Fig. 4 & \texttt{make\_figures\_and\_tables.py} & Pantheon+ masked Hubble residuals & \texttt{fig04\_pantheon\_residuals.pdf}\\
Fig. 5 & \texttt{make\_figures\_and\_tables.py} & AIC/BIC values from all fits & \texttt{fig05\_model\_selection.pdf}\\
Fig. 6 & \texttt{derived\_predictions.py}, \texttt{make\_figures\_and\_tables.py} & growth closure from Eq.~\eqref{eq:growth} & \texttt{fig06\_growth\_Dz.pdf}\\
\bottomrule
\end{tabular}
\end{table}

\subsection{Numerical validation}

The numerical validation file \texttt{code/numerical\_validation.json} is generated by \texttt{code/numerical\_validation.py}. It compares the production Newton solver against an independent bisection implementation on $0\le z\le3$ for the \LCDM limit, the main R1 solution and the main R2 solution. The maximum scaled implicit-equation residuals are
\[
1.65\times10^{-16}\quad(\LCDM),\qquad
2.02\times10^{-16}\quad({\rm R1}),\qquad
1.82\times10^{-16}\quad({\rm R2}).
\]
The maximum relative Newton-versus-bisection differences are $2.22\times10^{-16}$ for \LCDM and $3.33\times10^{-16}$ for both R1 and R2. The \LCDM analytic branch validates the $\theta=2$ limit with maximum relative $E(z)$ error $2.22\times10^{-16}$ and gives $j_0=0.9999979$. A distance-integration check at $z=2$ gives $D_C=5256.50471\,{\rm Mpc}$ by trapezoidal integration and $5256.50468\,{\rm Mpc}$ by Simpson integration, a relative difference of $5.03\times10^{-9}$.

\subsection{Independent verification}

The independent verification is algorithmic rather than cosmetic. The production solver uses Newton iteration initialized on the positive \LCDM-connected branch and falls back to bisection if the scaled residual fails. The validation solver uses bisection directly and never calls the Newton update. Agreement at the $10^{-16}$ level on the production redshift domain demonstrates that the reported background values are not artifacts of a branch jump or Newton convergence failure.

\subsection{Boltzmann and early-universe calculation status}

\texttt{code/boltzmann\_camb.py} adds a CAMB reference calculation for the \LCDM best-fit late-time parameters. It computes the early-universe baseline that replaces the purely fixed sound-horizon assumption in the audit trail:
\[
z_{\rm drag}=1060.010,\qquad
r_{\rm drag}=146.815\,{\rm Mpc},\qquad
z_{\rm eq}=3429.040,\qquad
t_0=13.655\,{\rm Gyr}.
\]
It writes \texttt{tables/camb\_lensed\_cls\_lcdm\_reference.csv} and \texttt{tables/camb\_matter\_power\_lcdm\_reference.csv}. This is a real Boltzmann calculation for the reference \LCDM sector. It is not yet a complete R1 Boltzmann likelihood, because a genuine R1 implementation must modify the background and perturbation modules so that CAMB/CLASS evolves the implicit R-sector self-consistently through radiation, baryons, neutrinos, recombination and perturbations. Approximating R1 by a CPL dark-energy form inside CAMB would not be the same theory and is therefore not claimed as a result.

\subsection{Statistical robustness status}

The current release contains deterministic best-fit and information-criterion comparisons for the public late-time likelihoods. It does not yet contain a completed MCMC chain, nested-sampling evidence, profile likelihood, corner plot, posterior covariance matrix or prior-sensitivity scan. These objects must be generated before making a Nature-level claim that R-Universe decisively supersedes \LCDM. The manuscript therefore treats the AIC preference as an empirical signal, not as a final Bayesian model-selection result.

\subsection{Dataset robustness status}

The release contains three fitted combinations: Pantheon+ + DESI DR1 + chronometers, Pantheon+ + DESI DR2 + chronometers, and DES-Dovekie + DESI DR2 + chronometers. Leave-one-BAO-point jackknife, bootstrap resampling, SN-only, BAO-only, chronometer-only, and no-chronometer fits are specified as required robustness products but are not yet claimed as completed in this draft. This prevents the audit record from overstating the present calculation.

\section{Model selection}

Model comparison uses
\begin{equation}
{\rm AIC}=\chi^2+2k,\qquad
{\rm BIC}=\chi^2+k\ln N,
\end{equation}
with $k=2$ for \LCDM, $k=3$ for R1 and $k=4$ for R2 \citep{Akaike1974,Schwarz1978}. Negative $\Delta$ values favor R-Universe relative to \LCDM.

\section{Figures}

\input{graf.tex}

\section{Numerical results}

\subsection{Pantheon+ full covariance + DESI DR1 + chronometers}

\begin{table}[H]
\centering
\caption{Pantheon+ full covariance + DESI DR1 BAO + cosmic chronometers.}
\begin{tabular}{lrrrrrrrr}
\toprule
Model & $k$ & $H_0$ & $\Omega_{\rm m0}$ & $\theta$ & $\nu$ & $\chi^2$ & $\Delta{\rm AIC}$ & $\Delta{\rm BIC}$\\
\midrule
\LCDM & 2 & 68.413 & 0.3116 & 2.000 & 0.000 & 1415.806 & 0 & 0\\
R1 & 3 & 68.050 & 0.2936 & 1.627 & 0.000 & 1413.759 & -0.047 & +5.345\\
R2 & 4 & 68.119 & 0.3169 & 3.727 & 1.314 & 1412.395 & +0.589 & +11.373\\
\bottomrule
\end{tabular}
\end{table}

The full Pantheon+ covariance weakens the earlier diagonal-covariance signal. R1 improves $\chi^2$, but the AIC gain is nearly zero and BIC disfavors it.

\subsection{Pantheon+ full covariance + DESI DR2 + chronometers}

\begin{table}[H]
\centering
\caption{Main fit: Pantheon+ full covariance + DESI DR2 BAO + cosmic chronometers.}
\begin{tabular}{lrrrrrrrr}
\toprule
Model & $k$ & $H_0$ & $\Omega_{\rm m0}$ & $\theta$ & $\nu$ & $\chi^2$ & $\Delta{\rm AIC}$ & $\Delta{\rm BIC}$\\
\midrule
\LCDM & 2 & 68.631 & 0.3059 & 2.000 & 0.000 & 1414.090 & 0 & 0\\
R1 & 3 & 67.975 & 0.2928 & 1.615 & 0.000 & 1410.378 & -1.712 & +3.681\\
R2 & 4 & 67.894 & 0.3045 & 2.408 & 0.508 & 1409.444 & -0.646 & +10.139\\
\bottomrule
\end{tabular}
\end{table}

For R1, the component breakdown is
\begin{align}
\Delta\chi^2_{\rm SN}&=-2.503,\\
\Delta\chi^2_{\rm BAO}&=-1.270,\\
\Delta\chi^2_{\rm CC}&=+0.061.
\end{align}
Thus the signal is shared by supernovae and BAO rather than coming from a single data block.

\subsection{DES-Dovekie + DESI DR2 + chronometers}

\begin{table}[H]
\centering
\caption{Independent supernova cross-check: DES-Dovekie STAT+SYS + DESI DR2 BAO + cosmic chronometers.}
\begin{tabular}{lrrrrrrrr}
\toprule
Model & $k$ & $H_0$ & $\Omega_{\rm m0}$ & $\theta$ & $\nu$ & $\chi^2$ & $\Delta{\rm AIC}$ & $\Delta{\rm BIC}$\\
\midrule
\LCDM & 2 & 68.544 & 0.3076 & 2.000 & 0.000 & 1659.504 & 0 & 0\\
R1 & 3 & 67.944 & 0.2916 & 1.593 & 0.000 & 1655.291 & -2.213 & +3.317\\
R2 & 4 & 67.819 & 0.3131 & 3.244 & 1.074 & 1652.965 & -2.540 & +8.521\\
\bottomrule
\end{tabular}
\end{table}

The DES-Dovekie cross-check reproduces the direction of the R1 signal outside the Pantheon+ pipeline. It remains AIC-favored but not BIC-favored.

\section{Falsifiable predictions}

For the main R1 branch,
\begin{align}
\theta &= 1.61484,\\
q_0 &= -0.49164,\\
j_0 &= 0.93419,\\
w_R(0)&=-0.93473,\\
w_R(1)&=-0.85249,\\
H_\infty/H_0&=0.80695.
\end{align}
The strongest null tests are:
\begin{enumerate}
\item $\theta=2$ must be recovered if future data prefer \LCDM;
\item ${\rm Om}(z)$ must show the predicted R1 drift rather than a constant value;
\item the jerk must deviate from $j_0=1$ at the level implied by the fitted $\theta$;
\item the $fD$ suppression and ISW proxy must survive a full perturbation calculation;
\item a global MCMC or nested-sampling analysis must preserve the AIC gain and ideally cross the BIC threshold.
\end{enumerate}

\section{What is complete and what remains open}

The late-time chain is complete: the physical principle is stated, the background equation is closed, the \LCDM limit is exact, all likelihood observables are computed, the public data sources are named, and the numerical results are generated by code.

The fundamental theory chain is not yet complete. A final replacement of \LCDM requires:
\begin{enumerate}
\item a covariant action or complete field equations for the R-sector;
\item a theoretical calculation of $\theta$ from a specified RG stability matrix;
\item a ghost and gradient-stability analysis;
\item a CLASS/CAMB implementation including radiation, baryons, neutrinos and recombination;
\item a full CMB likelihood;
\item full MCMC or nested-sampling evidence.
\end{enumerate}
These are not optional. They are the next falsification gates.

\section{Conclusion}

R-Universe R1 is a closed, reproducible, one-parameter late-time extension of flat \LCDM. It exactly contains \LCDM at $\theta=2$, computes all late-time distance observables, uses full covariance where available, includes an independent DES-Dovekie supernova cross-check, and gives explicit growth and CMB-proxy predictions under a stated minimal closure.

The empirical result is precise. Pantheon+ full covariance + DESI DR2 + chronometers gives $\Delta\chi^2=-3.7117$ and $\Delta{\rm AIC}=-1.7117$. DES-Dovekie + DESI DR2 + chronometers gives $\Delta\chi^2=-4.2133$ and $\Delta{\rm AIC}=-2.2133$. In both cases BIC remains positive. The model is therefore not yet a definitive replacement for \LCDM, but it is a serious AIC-supported, falsifiable extension whose next required test is a full Boltzmann-level and MCMC analysis.

\section*{Data availability}

All observational inputs used in this work are public and the exact small analysis files used by the scripts are included in \texttt{data/raw/}. Pantheon+ is available from the PantheonPlusSH0ES data release \citep{PantheonPlusDataRelease,Brout2022PantheonPlus}. DESI BAO DR1 and DR2 vectors and covariance matrices are available through the public DESI/Cobaya BAO data repository \citep{DESI2024VI,DESIDR2BAO,CobayaBAOData}. DES-Dovekie / DES-SN5YR distances and covariance matrices are available from the DES-SN5YR public release and Zenodo archive \citep{DES2024SN5YR,Sanchez2024DESSN,DESSN5YRRepo}. The chronometer and compressed growth/lensing inputs used for the diagnostic checks are listed explicitly in the code and/or manifest. Exact filenames, masks, data-vector sizes, release/access metadata, license status and SHA256 checksums are recorded in Section~\ref{sec:data-provenance} and \texttt{data\_manifest/data\_sources.md}.

\section*{Code availability}

The complete analysis pipeline, numerical solvers, likelihood implementation, result JSON files, CSV tables and scripts required to reproduce all figures and tables are publicly available at \repo. The manuscript-associated release tag is \release. A permanent Zenodo DOI should be minted from the \release\ GitHub release before journal submission; no DOI is fabricated in this draft. The repository records the Git history, and the release tag fixes the audit target corresponding to this manuscript.

\section*{Reproducibility statement}

From the released package root, the complete late-time calculation is reproduced by running:
\begin{verbatim}
python3 code/extended_fit.py
python3 code/des_dovekie_fit.py
python3 code/derived_predictions.py
python3 code/make_figures_and_tables.py
\end{verbatim}
The expected primary numerical output is \texttt{tables/all\_model\_fits.csv}. The expected BAO diagnostic output is \texttt{tables/desi\_dr2\_bao\_predictions.csv}. All figure panels included through \texttt{graf.tex} are regenerated in \texttt{figures/}. The calculations were rerun before release generation and the citation/figure references in the \LaTeX\ source were checked for consistency.

\appendix

\section{Computational and data provenance}

\subsection{Parameter vectors and minimization}

The fitted parameter vectors are
\[
\Theta_{\Lambda{\rm CDM}}=(H_0,\Omega_{\rm m0}),\qquad
\Theta_{\rm R1}=(H_0,\Omega_{\rm m0},\theta),\qquad
\Theta_{\rm R2}=(H_0,\Omega_{\rm m0},\theta,\nu).
\]
For each trial vector the solver first computes the positive physical branch of $E(z)$. \LCDM is evaluated analytically. R1 and R2 are evaluated by solving the implicit background equations on the redshift points required by the likelihood and by quadrature for distances. The solution branch is initialized at the \LCDM-connected root and rejected if the denominator in Eq.~\eqref{eq:eprime} crosses a singular value on the evaluated redshift domain.

The likelihood is not optimized independently per dataset. The objective minimized is always the full combined statistic
\[
\chi^2_{\rm tot}(\Theta)=
\chi^2_{\rm SN}(\Theta)+
\chi^2_{\rm BAO}(\Theta)+
\chi^2_{\rm CC}(\Theta).
\]
The fitted $\chi^2$ values are then decomposed by block after the best-fit vector is identified. This is why the component shifts in the main fit add to the quoted total shift.

\subsection{Pantheon+ calculation record}

The Pantheon+ calculation uses the following deterministic pipeline:
\begin{enumerate}
\item read \texttt{pantheon\_plus.dat};
\item read \texttt{pantheon\_plus\_stat\_sys.cov};
\item construct the boolean mask \texttt{is\_calibrator == False} and $z_{\rm HD}>0.01$;
\item apply the same mask to the data vector and covariance rows/columns;
\item compute $D_C(z_{\rm HD})$ and $D_L=(1+z_{\rm hel})D_C$;
\item compute $\mu_{\rm th}=5\log_{10}(D_L/{\rm Mpc})+25$;
\item form residuals $r=\mu_{\rm obs}-\mu_{\rm th}$;
\item analytically marginalize the additive intercept using Eq.~\eqref{eq:snchi};
\item return $\chi^2_{\rm SN}$ to the global objective.
\end{enumerate}
The final retained Pantheon+ size is $1580$. In the main Pantheon+ + DESI DR2 + chronometer fit, the supernova block contributes $1388.620$ for \LCDM and $1386.116$ for R1 at their respective global best fits.

\subsection{DES-Dovekie calculation record}

The DES-Dovekie calculation is intentionally parallel to Pantheon+:
\begin{enumerate}
\item read \texttt{DES-Dovekie\_HD.csv};
\item read the \texttt{STAT+SYS.npz} covariance matrix;
\item preserve the published Hubble-diagram ordering;
\item compute $D_L$ from the candidate cosmology;
\item analytically marginalize the additive intercept;
\item combine the resulting $\chi^2_{\rm SN}$ with DESI DR2 BAO and chronometers.
\end{enumerate}
The DES-Dovekie cross-check gives $\Delta\chi^2=-4.2133$ and $\Delta{\rm AIC}=-2.2133$ for R1 relative to \LCDM. Its agreement in sign with Pantheon+ is an important robustness check because it uses a different supernova pipeline and covariance product.

\subsection{DESI DR2 BAO calculation record}

The DESI DR2 BAO vector is evaluated as follows:
\begin{enumerate}
\item read \texttt{desi\_gaussian\_bao\_ALL\_GCcomb\_mean.txt};
\item read \texttt{desi\_gaussian\_bao\_ALL\_GCcomb\_cov.txt};
\item for each row identify whether the observable is $D_V/r_d$, $D_M/r_d$ or $D_H/r_d$;
\item compute $D_M$, $D_H$ and $D_V$ from the same $E(z)$ used in the supernova likelihood;
\item fix $r_d=147.09\,{\rm Mpc}$;
\item build the prediction vector in the published order;
\item evaluate the full-covariance quadratic form.
\end{enumerate}
For the main fit, the BAO block contributes $10.908$ for \LCDM, $9.638$ for R1 and $8.710$ for R2. The diagonal pulls in \texttt{tables/desi\_dr2\_bao\_predictions.csv} are diagnostic only; the fit uses the full covariance matrix.

\subsection{Chronometer calculation record}

The chronometer block is a transparent diagonal likelihood:
\[
\chi^2_{\rm CC}=\sum_{i=1}^{31}\frac{[H_i-H_0E(z_i)]^2}{\sigma_{H,i}^2}.
\]
The numerical triplets are embedded in the fitting scripts. This makes the code self-auditing but also fixes the limitation: the calculation does not yet include a full correlated chronometer systematic covariance. In the main fit, the chronometer block changes by only $+0.061$ between \LCDM and R1, so the R1 improvement is not driven by chronometer tuning.

\subsection{Derived predictions calculation record}

\texttt{code/derived\_predictions.py} reads the main best-fit vectors from \texttt{code/extended\_results.json}. It then computes:
\begin{itemize}
\item $q_0$ from $E'(z)$;
\item $j_0$ by differentiating $q(z)$ numerically and validating \LCDM with $j_0=0.999998$;
\item $w_R(0)$ and $w_R(1)$ using Eq.~\eqref{eq:wR};
\item the R1 future limit $H_\infty/H_0=(1-\Omega_{\rm m0})^{1/\theta}$;
\item the Sandage--Loeb velocity drift at $z=2$;
\item the normalized growth function $D(z)$ and growth rate $f(z)$ from Eq.~\eqref{eq:growth};
\item a minimal late-ISW source proxy from the redshift derivative of $D(z)(1+z)$.
\end{itemize}
The main R1 values are $\theta=1.61484$, $q_0=-0.49164$, $j_0=0.93419$, $w_R(0)=-0.93473$, $w_R(1)=-0.85249$, $H_\infty/H_0=0.80695$, and a maximum normalized $fD$ suppression of $3.42\%$ at $z\simeq0.33$.

\bibliographystyle{unsrtnat}
\bibliography{references}

\end{document}
