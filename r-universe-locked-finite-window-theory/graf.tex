% Figure block for the locked R-Universe finite-window theory.
% Filenames intentionally match files in the Overleaf project root.

\begin{figure}[htbp]
\centering
\includegraphics[draft=false,width=0.98\linewidth]{figures/fig01_locked_r_universe_background.pdf}
\caption{Direct background integration of the locked finite-window R-Universe equations. The upper-left panel shows the window $W(R)$ and the geometric calibration $F(R)$ generated by the dynamical calibration field. The upper-right panel shows the exact R-critical closure $\Omega_m^R+\Omega_r^R+\Omega_R=1$. The lower-left panel shows the self-generated expansion rate normalized at the reporting surface. The lower-right panel shows the deceleration parameter $q$ and the unscreened coupling proxy $G_{\rm unscreened}/G=1/F$. The plotted branch is not fitted to an external $H(z)$ curve; it follows from the locked functions and the calibration equation.}
\label{fig:locked-background}
\end{figure}
